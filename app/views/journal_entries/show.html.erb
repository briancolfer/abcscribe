<div class="w-full max-w-4xl mx-auto">
  <div class="flex justify-between items-center mb-8">
    <h1 class="text-3xl font-bold text-gray-900">Journal Entry</h1>
    <div class="flex space-x-3">
      <%= link_to "Edit", edit_journal_entry_path(@journal_entry), class: "bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded" %>
      <%= link_to "Delete", @journal_entry, method: :delete, 
                 confirm: "Are you sure you want to delete this entry?", 
                 class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" %>
      <%= link_to "Back to Entries", journal_entries_path, class: "bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded" %>
    </div>
  </div>
  
  <div class="bg-white border border-gray-200 rounded-lg shadow-sm">
    <div class="p-6">
      <div class="text-sm text-gray-500 mb-6">
        Created on <%= @journal_entry.created_at.strftime("%B %d, %Y at %I:%M %p") %>
        <% if @journal_entry.updated_at != @journal_entry.created_at %>
          • Last updated <%= @journal_entry.updated_at.strftime("%B %d, %Y at %I:%M %p") %>
        <% end %>
      </div>
      
      <p>Tags:</p>
      <ul class="flex flex-wrap space-x-2">
        <% @journal_entry.tags.each do |tag| %>
          <li class="tag bg-blue-200 text-blue-800 px-2 py-1 rounded"><%= tag.name %></li>
        <% end %>
      </ul>
      
      <div class="space-y-6">
        <div class="bg-red-50 border-l-4 border-red-400 p-6 rounded-r-lg">
          <h3 class="text-lg font-semibold text-red-800 mb-3">Antecedent</h3>
          <p class="text-gray-700 leading-relaxed"><%= simple_format(@journal_entry.antecedent) %></p>
        </div>
        
        <div class="bg-blue-50 border-l-4 border-blue-400 p-6 rounded-r-lg">
          <h3 class="text-lg font-semibold text-blue-800 mb-3">Behavior</h3>
          <p class="text-gray-700 leading-relaxed"><%= simple_format(@journal_entry.behavior) %></p>
        </div>
        
        <div class="bg-green-50 border-l-4 border-green-400 p-6 rounded-r-lg">
          <h3 class="text-lg font-semibold text-green-800 mb-3">Consequence</h3>
          <p class="text-gray-700 leading-relaxed"><%= simple_format(@journal_entry.consequence) %></p>
        </div>
      </div>
    </div>
  </div>
  
  <div class="mt-8 text-center">
    <p class="text-gray-600 text-sm">
      ABC Analysis helps identify patterns in behavior by examining what happens before (Antecedent), 
      during (Behavior), and after (Consequence) specific events.
    </p>
  </div>
</div>
